# include <stdio.h>
   int main(){
     char choice;
     int perimeter,lenght,breadth,side,pie,radius,area;
     printf("to find area,and perimeter enter your choice\n");
     printf("enter 'r' for rectangle, 's' for square, 't' for triangle and 'c' for circle");
     scanf("%c",&choice);

      if(choice='y'){
         printf("enter lenght and breadth \n");
         scanf("%d%d",&lenght,&breadth);
         perimeter=2*(lenght+breadth);
      printf("the perimeter of rectangle %d",perimeter) ;
      area=1/2*
      }
 else if("")

   }
